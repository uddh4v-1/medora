# Workspace-level ignores. Per-package patterns live in:
#   frontend/.gitignore
#   backend/.gitignore

# Editor / OS
.DS_Store
Thumbs.db
*.swp

# Logs
*.log

# Env files at every level (each package can opt-in if needed)
.env
.env.*
!*.example
